apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.triton.tritonGrpc.name }}
  labels:
    {{- include "triton.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.global.triton.tritonGrpc.port }}
      targetPort: {{ .Values.triton.grpcPort }}
      protocol: TCP
      name: "grpc-port"
  selector:
    app: {{ template "triton.name" . }}
    release: {{ .Release.Name }}
