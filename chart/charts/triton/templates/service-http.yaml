apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.triton.tritonHttp.name }}
  labels:
    {{- include "triton.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.global.triton.tritonHttp.port }}
      targetPort: {{ .Values.triton.httpPort }}
      protocol: TCP
      name: "http-port"
  selector:
    app: {{ template "triton.name" . }}
    release: {{ .Release.Name }}
